# Backend API base (vite dev proxy also forwards /api → localhost:8000)
VITE_API_URL=/api

# Clerk publishable key — written automatically by `clerk init --framework react`
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...
